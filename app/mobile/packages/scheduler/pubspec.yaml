name: scheduler
description: FSRS scheduling. Pure Dart, zero IO, no ambient clock, no randomness.
publish_to: none
version: 0.1.0

environment:
  sdk: ^3.8.0

dependencies:
  domain:
    path: ../domain
  fsrs: ^2.0.1

dev_dependencies:
  test: ^1.25.0
