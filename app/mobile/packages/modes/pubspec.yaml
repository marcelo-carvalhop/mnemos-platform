name: modes
description: Alternative study modes (§5.9) — multiple choice, leech drill, timed simulado and audio.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.8.0

dependencies:
  domain:
    path: ../domain
  drift: ^2.20.0
  scheduler:
    path: ../scheduler
  session:
    path: ../session
  store:
    path: ../store

dev_dependencies:
  lints: ^5.0.0
  sqlite3: ^2.4.0
  test: ^1.25.0
