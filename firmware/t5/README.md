# Mnemos Terminal — LILYGO T5

Espaço reservado para a implementação do terminal Mnemos sobre LILYGO T5 4.7". O port deve preservar os contratos definidos em `../../spec/` e não introduzir formatos de sincronização específicos do hardware.

Enquanto a implementação ainda não existe, os diretórios `include/`, `src/`, `test/` e `data/` permanecem versionados por arquivos `.gitkeep`.
