# Fixture canônico — Redes de Computadores

Este diretório contém um único deck de demonstração com 10 cards básicos. `redes-10-cards.sync.json` representa o snapshot completo; `deck.json`, `cards.json` e `states.json` expõem as mesmas partes separadamente para facilitar testes de integração.
