# Cliente móvel

O aplicativo Flutter fica em `app/mobile/`. Ele mantém persistência local, autoria, estudo, provisionamento do terminal e sincronização. Os pacotes Dart que compõem seu domínio permanecem em `app/mobile/packages/` porque o `pubspec.yaml` utiliza referências relativas nessa estrutura.
