# Terminais

`firmware/cyd/` contém o protótipo atual e `firmware/t5/` está reservado para o hardware e-paper. Código verdadeiramente independente de placa poderá futuramente ser extraído para `firmware/common/`, mas apenas quando existir reutilização real validada nas duas plataformas.
